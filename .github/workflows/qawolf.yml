name: QAWolf
on: [deployment_status]
jobs:
  qawolf:
    if: github.event.deployment_status.state == 'success'
    runs-on: ubuntu-latest
    steps:
      - run: URL=${{ github.event.deployment_status.target_url }} npm run qawolf
