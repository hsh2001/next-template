import styled from 'styled-components';

const FooterContainer = styled.footer``;

const Footer: React.FC = () => {
  return <FooterContainer>Footer here</FooterContainer>;
};

export default Footer;
