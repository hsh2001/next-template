import styled from 'styled-components';

const HeaderContainer = styled.header``;

const Header: React.FC = () => {
  return <HeaderContainer>Header here</HeaderContainer>;
};

export default Header;
